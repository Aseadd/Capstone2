// import likesApi from '../likesApi';

// it('Testing the likes API', async () => {
//   const allLikes = await likesApi.getAllLikes();
//   const { count } = allLikes.reduce(
//     (accumulator, current) => accumulator + current.likes,
//     0
//   );
//   console.log(count);
//   expect(count).toEqual(50);
// });
